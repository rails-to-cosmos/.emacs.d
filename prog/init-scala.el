;;; init-scala.el --- provide scala configuration.
;;; Commentary:
;;; Code:

(defun init-scala () "Initialize scala environment with my configuration." (interactive))

(use-package scala-mode
  :ensure t)

(provide 'init-scala)
;;; init-scala.el ends here
