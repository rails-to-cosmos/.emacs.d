[package]
name = "redis-mod"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "redis_mod"
crate-type = ["cdylib"]

[dependencies]
emacs = "0.19"
redis = { version = "0.27", default-features = false }
once_cell = "1"
shlex = "1"

[profile.release]
lto = "thin"
codegen-units = 1
